'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';

const Navigation = ({ scrollDirection, currentWidth, setSelection, selection }) => { 
  return (
    <></>
  );
};

export default Navigation;
